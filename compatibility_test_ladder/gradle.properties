org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
# level9_gms depends on the Play services and Firebase client libraries, which are
# AndroidX artefacts. The earlier ladder modules reference no support or AndroidX class,
# so flipping this changes nothing for them.
android.useAndroidX=true
