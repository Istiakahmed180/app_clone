# Vendored prebuilt engine and its helper jar are binary: never text-diff, merge, or
# normalize line endings. They are regenerable from the pinned source commit; see
# android/app/libs/BCORE_SOURCE_COMMIT.txt.
*.aar   binary
*.jar   binary
*.so    binary
*.apk   binary
*.keystore binary
