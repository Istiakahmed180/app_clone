diff --git a/Bcore/src/main/java/top/niunaijun/blackbox/core/env/BEnvironment.java b/Bcore/src/main/java/top/niunaijun/blackbox/core/env/BEnvironment.java
index a40e626..dfd2986 100644
--- a/Bcore/src/main/java/top/niunaijun/blackbox/core/env/BEnvironment.java
+++ b/Bcore/src/main/java/top/niunaijun/blackbox/core/env/BEnvironment.java
@@ -139,6 +139,11 @@ public class BEnvironment {
         return new File(sVirtualRoot, "data/app/" + packageName + "/base.apk");
     }
 
+    /** Where a split install's extra APKs are kept inside the container. */
+    public static File getSplitApkFile(String packageName, String splitFileName) {
+        return new File(getAppDir(packageName), splitFileName);
+    }
+
     public static File getAppLibDir(String packageName) {
         return new File(getAppDir(packageName), "lib");
     }
diff --git a/Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/BPackage.java b/Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/BPackage.java
index 4ffcc08..216b89a 100644
--- a/Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/BPackage.java
+++ b/Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/BPackage.java
@@ -45,6 +45,12 @@ public class BPackage implements Parcelable {
     public ApplicationInfo applicationInfo;
     public String mVersionName;
     public String baseCodePath;
+    /**
+     * Extra APKs of a split install (the ABI split carries the native libraries, the
+     * density split the resources). Without these the guest only ever sees base.apk, so
+     * anything native silently never initialises.
+     */
+    public String[] splitCodePaths;
 
     public int mSharedUserLabel;
     
@@ -134,6 +140,7 @@ public class BPackage implements Parcelable {
         this.applicationInfo = aPackage.applicationInfo;
         this.mVersionName = aPackage.mVersionName;
         this.baseCodePath = aPackage.baseCodePath;
+        this.splitCodePaths = aPackage.splitCodePaths;
         this.mSharedUserLabel = aPackage.mSharedUserLabel;
         this.configPreferences = aPackage.configPreferences;
         this.reqFeatures = aPackage.reqFeatures;
@@ -224,6 +231,7 @@ public class BPackage implements Parcelable {
         this.applicationInfo = in.readParcelable(ApplicationInfo.class.getClassLoader());
         this.mVersionName = in.readString();
         this.baseCodePath = in.readString();
+        this.splitCodePaths = in.createStringArray();
         this.mSharedUserLabel = in.readInt();
         this.configPreferences = in.createTypedArrayList(ConfigurationInfo.CREATOR);
         this.reqFeatures = in.createTypedArrayList(FeatureInfo.CREATOR);
@@ -714,6 +722,7 @@ public class BPackage implements Parcelable {
         dest.writeParcelable(this.applicationInfo, flags);
         dest.writeString(this.mVersionName);
         dest.writeString(this.baseCodePath);
+        dest.writeStringArray(this.splitCodePaths);
         dest.writeInt(this.mSharedUserLabel);
         dest.writeTypedList(this.configPreferences);
         dest.writeTypedList(this.reqFeatures);
diff --git a/Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/PackageManagerCompat.java b/Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/PackageManagerCompat.java
index ab9983b..8a8cbd2 100644
--- a/Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/PackageManagerCompat.java
+++ b/Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/PackageManagerCompat.java
@@ -304,6 +304,13 @@ public class PackageManagerCompat {
         ai.processName = BPackageManagerService.fixProcessName(p.packageName, ai.packageName);
         ai.publicSourceDir = sourceDir;
         ai.sourceDir = sourceDir;
+        // Hand the guest its split APKs as well. Leaving these null means the guest's
+        // ClassLoader only ever opens base.apk, so classes and native libraries that live in
+        // a split are invisible -- the app runs but anything depending on them hangs.
+        if (p.splitCodePaths != null && p.splitCodePaths.length > 0) {
+            ai.splitSourceDirs = p.splitCodePaths;
+            ai.splitPublicSourceDirs = p.splitCodePaths;
+        }
         ai.uid = p.mExtras.appId;
 
 
diff --git a/Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/installer/CopyExecutor.java b/Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/installer/CopyExecutor.java
index b8325a2..0d76cd2 100644
--- a/Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/installer/CopyExecutor.java
+++ b/Bcore/src/main/java/top/niunaijun/blackbox/core/system/pm/installer/CopyExecutor.java
@@ -4,11 +4,13 @@ package top.niunaijun.blackbox.core.system.pm.installer;
 import java.io.File;
 import java.io.IOException;
 
+import top.niunaijun.blackbox.BlackBoxCore;
 import top.niunaijun.blackbox.core.env.BEnvironment;
 import top.niunaijun.blackbox.core.system.pm.BPackageSettings;
 import top.niunaijun.blackbox.entity.pm.InstallOption;
 import top.niunaijun.blackbox.utils.FileUtils;
 import top.niunaijun.blackbox.utils.NativeUtils;
+import top.niunaijun.blackbox.utils.Slog;
 
 
 public class CopyExecutor implements Executor {
@@ -17,7 +19,51 @@ public class CopyExecutor implements Executor {
     public int exec(BPackageSettings ps, InstallOption option, int userId) {
         try {
             if (!option.isFlag(InstallOption.FLAG_SYSTEM)) {
-                NativeUtils.copyNativeLib(new File(ps.pkg.baseCodePath), BEnvironment.getAppLibDir(ps.pkg.packageName));
+                File base = new File(ps.pkg.baseCodePath);
+                File libDir = BEnvironment.getAppLibDir(ps.pkg.packageName);
+                NativeUtils.copyNativeLib(base, libDir);
+
+                // A split install keeps its native libraries in the ABI split
+                // (split_config.arm64_v8a.apk), not in base.apk, so extracting only from base
+                // leaves lib/ empty and anything native silently never initialises -- VLC, for
+                // one, then shows "Loading..." forever.
+                //
+                // The split paths must come from the host PackageManager, not from listing
+                // base.apk's directory: /data/app/<pkg>/ belongs to another app and
+                // File.listFiles() on it returns null for us, which is why a directory scan
+                // finds nothing at all.
+                String[] splits = null;
+                try {
+                    splits = BlackBoxCore.getPackageManager()
+                            .getApplicationInfo(ps.pkg.packageName, 0).splitSourceDirs;
+                } catch (Throwable ignored) {
+                    // Not installed on the host (an imported APK); it has no splits.
+                }
+                Slog.i("SplitDiag", "pkg=" + ps.pkg.packageName
+                        + " base=" + ps.pkg.baseCodePath
+                        + " baseReadable=" + base.canRead()
+                        + " splits=" + (splits == null ? "null" : String.valueOf(splits.length)));
+                if (splits != null) {
+                    for (String sp : splits) {
+                        Slog.i("SplitDiag", "  split=" + sp + " readable=" + new File(sp).canRead());
+                    }
+                }
+                if (splits != null) {
+                    for (String split : splits) {
+                        if (split == null) {
+                            continue;
+                        }
+                        try {
+                            NativeUtils.copyNativeLib(new File(split), libDir);
+                            String[] got = libDir.list();
+                            Slog.i("SplitDiag", "  after " + new File(split).getName()
+                                    + " libCount=" + (got == null ? -1 : got.length));
+                        } catch (Throwable e) {
+                            Slog.e("SplitDiag", "  copyNativeLib failed for " + split, e);
+                        }
+                    }
+                    ps.pkg.splitCodePaths = splits;
+                }
             }
         } catch (Exception e) {
             e.printStackTrace();
